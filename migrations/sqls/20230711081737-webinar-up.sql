/* Replace with your SQL commands */
create table webinar(
    id serial primary key,
    title varchar,
    description varchar,
    date varchar,
    category varchar,
    image varchar,
    url varchar

)